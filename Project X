   var email = " SupporT@hexlet.io\n";
        email = email.trim();// метод позволяет в Java удалить пробелы в начале и конце строки 
        email = email.toLowerCase(); //метод переводит строку в нижний регистр

        System.out.print(email);
